# Core dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Enhanced fuzzy matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.20.0  # For faster fuzzy matching

# NLP support (optional but recommended)
spacy>=3.5.0
# Run after install: python -m spacy download en_core_web_sm

# JavaScript rendering (choose one or both)
playwright>=1.30.0  # Recommended
# Run after install: playwright install chromium

selenium>=4.8.0  # Alternative
# Also need ChromeDriver: https://chromedriver.chromium.org/

# Embeddings for better classification (optional)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Additional utilities
python-dateutil>=2.8.0