# Enhanced Healthcare Company Directory Scraper - Dependencies
# ============================================================

# Core HTTP library for web requests
requests>=2.28.0

# HTML parsing library for extracting data from web pages
beautifulsoup4>=4.11.0

# Fast XML/HTML parser (improves BeautifulSoup performance)
lxml>=4.9.0

# Optional: Selenium for JavaScript-heavy websites
# Comment out if you don't need Selenium support
selenium>=4.15.0

# Optional: Automatically manages ChromeDriver installation
# Only needed if using Selenium
webdriver-manager>=4.0.0

# Note: The scraper will work without these optional dependencies
# Use --no-selenium flag if you don't install Selenium packages